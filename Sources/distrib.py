def distrib(LJ,J,P):
    if len(P) % len(LJ) # verif qu'on peut jouer
    for c in range(2):
        for i in LJ:
            J[i][1].append(P.pop()) # On ajoute une carte dans la liste de carte du joueur i
